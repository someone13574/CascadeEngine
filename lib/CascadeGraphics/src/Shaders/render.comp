#version 450

layout(local_size_x = 32, local_size_y = 32) in;

// bindings
layout(binding = 0, rgba8) uniform image2D render_target;

// code
void main()
{
    ivec2 id = ivec2(gl_GlobalInvocationID.xy);

    vec4 color = vec4(id.x & id.y);

    imageStore(render_target, id, color);
}