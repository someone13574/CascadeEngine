#version 460

layout(local_size_x = 32, local_size_y = 32) in;

// bindings
layout(binding = 0, rgba8) uniform image2D render_target;

void main()
{
    vec4 color = vec4(gl_GlobalInvocationID.x ^ gl_GlobalInvocationID.y);
    imageStore(render_target, ivec2(gl_GlobalInvocationID.xy), color);
}